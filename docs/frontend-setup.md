# Frontend Setup

## Local development

```bash
cd frontend
npm ci
npm run dev
```

## Environment

Copy `frontend/.env.example` to a local env file and configure `VITE_API_BASE_URL` for your backend instance.
