[project]
name = "neuropulse-backend"
version = "0.1.0"
description = "FastAPI backend for the NeuroPulse monitoring platform."
requires-python = ">=3.12"
dependencies = []

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I"]
